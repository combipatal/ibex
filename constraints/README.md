# Constraints

Initial planned SDC:

```text
ibex_mini_soc_10ns.sdc
```

Initial clock target:

```text
10 ns
```

Current status:

```text
Initial baseline SDC created for clk_i, reset false path, GPIO/IMEM preload input delays, and simple output delays.
```
